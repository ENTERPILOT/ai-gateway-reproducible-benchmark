# How the harness talks to OmniRoute. Sourced by run-on-instance.sh.
IMAGE=diegosouzapw/omniroute:latest   # default image; override with OMNIROUTE_IMAGE
PORT=20128
MODEL=mock/gpt-4o-mini                # <provider prefix>/<model>: the mock node seeded by bootstrap.mjs
MESSAGES_PATH=/v1/messages
HEADERS=()
